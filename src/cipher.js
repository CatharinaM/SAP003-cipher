window.cipher = {
  encode: encode,
  // decode: decode
};

function encode(offset, string){
  alert(offset);
  alert (string);

}

// function decode(offset, string){
  
// return

// }

// ((codigoDaLetraASC - codigDaletra + desloc) % tamDoAlfabeto + cod1aletra